{
	admin off
}

{$DOMAIN:localhost} {
	encode gzip zstd

	handle_path /api/* {
		reverse_proxy {$BACKEND_UPSTREAM:backend:8080}
	}

	handle {
		root * /usr/share/caddy
		try_files {path} /index.html
		file_server
	}
}
